<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_pikacu_coin"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_pikacu_coin"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project1">
        <activity android:name=".Activity_Easy" />
        <activity android:name=".Activity_Medium" />
        <activity android:name=".Activity_Hard" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>